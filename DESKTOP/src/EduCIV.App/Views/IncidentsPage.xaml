<UserControl x:Class="EduCIV.App.Views.IncidentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{DynamicResource ColorBg}">
    <UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Incidents" FontSize="28" FontWeight="Bold" FontFamily="{DynamicResource FontHeading}" Foreground="{DynamicResource ColorText}" VerticalAlignment="Center"/>
            <Button Content="Ajouter" Command="{Binding AddItemCommand}" Style="{StaticResource PrimaryButton}" Margin="16,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid Grid.Row="0" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                          CanUserSortColumns="True" CanUserResizeColumns="True" SelectionMode="Single"
                          RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                          Background="Transparent" BorderThickness="0"
                          VerticalGridLinesBrush="{DynamicResource ColorBorder}"
                          HorizontalGridLinesBrush="{DynamicResource ColorBorder}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" FontSize="12"/>
                        <DataGridTextColumn Header="Eleve" Binding="{Binding StudentId}" Width="120"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="100"/>
                        <DataGridTemplateColumn Header="Statut" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource Badge}">
                                                <Setter Property="Background" Value="{DynamicResource ColorSurface}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="EN_COURS">
                                                        <Setter Property="Background" Value="{DynamicResource ColorWarningBgBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="RESOLU">
                                                        <Setter Property="Background" Value="{StaticResource ColorSuccessBgBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="IGNORE">
                                                        <Setter Property="Background" Value="{StaticResource ColorInfoBgBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold" FontFamily="{StaticResource FontSans}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource ColorTextSecondary}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="EN_COURS">
                                                            <Setter Property="Foreground" Value="{DynamicResource ColorWarningTextBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="RESOLU">
                                                            <Setter Property="Foreground" Value="{StaticResource ColorSuccessTextBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="IGNORE">
                                                            <Setter Property="Foreground" Value="{StaticResource ColorInfoTextBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Modifier" Command="{Binding DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}" Style="{StaticResource GhostButton}" FontSize="11"/>
                                        <Button Content="Supprimer" Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}" Style="{StaticResource GhostButton}" FontSize="11"
                                                Foreground="{DynamicResource ColorErrorBrush}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Border Grid.Row="1" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Background="{DynamicResource ColorSurface}" BorderBrush="{DynamicResource ColorBorder}" BorderThickness="0,1,0,0" Padding="16" Margin="16">
                    <StackPanel>
                        <TextBlock Text="Nouvel incident" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ColorText}" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding SelectedItem.StudentId, Mode=TwoWay}" Style="{StaticResource ModernTextBox}" Tag="ID Eleve" Margin="0,0,8,8"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedItem.Type, Mode=TwoWay}" Style="{StaticResource ModernTextBox}" Tag="Type" Margin="8,0,0,8"/>
                            <TextBox Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding SelectedItem.Description, Mode=TwoWay}" Style="{StaticResource ModernTextBox}" Tag="Description" Margin="0,0,0,8"/>
                            <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Annuler" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}"/>
                                <Button Content="Enregistrer" Command="{Binding SaveItemCommand}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
