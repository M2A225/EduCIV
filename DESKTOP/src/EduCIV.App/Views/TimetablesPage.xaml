<UserControl x:Class="EduCIV.App.Views.TimetablesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{DynamicResource ColorBg}">
    <UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Emplois du temps" FontSize="28" FontWeight="Bold" FontFamily="{DynamicResource FontHeading}" Foreground="{DynamicResource ColorText}" VerticalAlignment="Center"/>
            <Button Content="Ajouter" Command="{Binding AddItemCommand}" Style="{StaticResource PrimaryButton}" Margin="16,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="0,0,0,16" Padding="16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filtrer par classe:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Text="{Binding SelectedClassId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Tag="ID Classe" Width="200"/>
                <Button Content="Charger" Command="{Binding LoadItemsCommand}" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource ModernCard}" Padding="0">
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                      CanUserSortColumns="True" CanUserResizeColumns="True" SelectionMode="Single"
                      RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                      Background="Transparent" BorderThickness="0"
                      VerticalGridLinesBrush="{DynamicResource ColorBorder}"
                      HorizontalGridLinesBrush="{DynamicResource ColorBorder}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" FontSize="12"/>
                    <DataGridTextColumn Header="Classe" Binding="{Binding ClassId}" Width="120"/>
                    <DataGridTextColumn Header="Enseignant" Binding="{Binding TeacherId}" Width="120"/>
                    <DataGridTextColumn Header="Matiere" Binding="{Binding SubjectId}" Width="120"/>
                    <DataGridTextColumn Header="Creneau" Binding="{Binding Slot}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
